# Contributing

Install the locked JavaScript dependencies and run the repository checks:

~~~bash
npm ci
npm test
npm run build
~~~

Keep seeded, estimated, and verified scenario data distinct. Add tests for changes to scoring, external inputs, exports, or operational actions. Preserve keyboard accessibility and reduced-motion behavior.

Do not commit credentials, private operational feeds, or unredacted screenshots.
